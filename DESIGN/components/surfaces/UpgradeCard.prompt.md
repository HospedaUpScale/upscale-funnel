Sits at the bottom of the sidebar: mark, title, quoted one-liner, full-width ink button.

<UpgradeCard onCta={openPlans} />

Body copy is written in quotes, first-person-plural voice. Set `window.FYNIX_ASSET_BASE` so the mark resolves from your page.
