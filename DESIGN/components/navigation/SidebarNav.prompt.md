216px sidebar on the canvas grey, grouped by uppercase eyebrow ("MAIN MENU", "PREFERENCE").

<SidebarNav sections={sections} active={page} onNavigate={setPage} header={<AccountSwitcher />} footer={<UpgradeCard />} />

The active item is a white 8px-radius chip with a hairline border and semibold label — not a coloured fill. `collapsed` narrows to a 72px icon rail.
