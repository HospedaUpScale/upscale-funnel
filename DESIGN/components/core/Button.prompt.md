The one button — pill-shaped by default, ink for primary actions and green for confirmations.

<Button variant="brand" iconRight="arrow-up-right">Send</Button>

Variants: primary (ink #062F28), brand (green #9FE870 with ink label — never white text), secondary (white + hairline border), soft (green tint), ghost, danger. Press state shrinks to 0.975; hover darkens one step. Use `pill={false}` only for full-width in-card confirms like Exchange.
