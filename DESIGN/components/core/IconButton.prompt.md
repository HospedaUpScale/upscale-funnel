Circular icon-only button for top bars, card headers and the composer tray.

<IconButton icon="bell" label="Notifications" badge variant="plain" />

Always circular. `bare` for inside card headers (the `more-horizontal` menu), `plain` for floating toolbars, `brand` for the "+" on the balance card. Minimum 32px in dense UI; use size="lg" (40px) on touch surfaces.
