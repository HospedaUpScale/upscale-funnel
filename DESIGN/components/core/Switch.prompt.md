Settings toggle. Green when on, grey when off, knob springs across.

<Switch checked={on} onChange={setOn} label="Two-factor authentication" description="Require a code at every sign-in." />

Intentional addition — the source case study never shows a settings screen, so this follows the system's control language (green fill, pill track, spring easing) rather than a screenshot.
