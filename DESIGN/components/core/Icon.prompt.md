Single outline icon — every glyph in the system comes from here, never inline hand-drawn SVG.

<Icon name="trending-up" size={16} color="var(--status-success-fg)" />

Names are Lucide names (kebab-case), loaded from the pinned jsDelivr copy of lucide-static and tinted with a CSS mask, so `color` works like text colour. Keep sizes at 14–16px inside controls and 16–18px in navigation.
