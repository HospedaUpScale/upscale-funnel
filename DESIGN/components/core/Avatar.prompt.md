Circular avatar; falls back to initials on the green AI-orb gradient.

<Avatar name="Jenny Wilson" size="md" ring />

Always circular. `ring` marks the current user. Sizes: xs 20 (table rows), sm 26, md 32 (top bar), lg 40, xl 56 (profile headers).
