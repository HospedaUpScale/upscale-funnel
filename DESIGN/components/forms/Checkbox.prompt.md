16px checkbox; ink fill with a white tick when checked.

<Checkbox checked={all} indeterminate={some} onChange={toggleAll} label="Select all" />

Used mainly in DataTable's select column. 6px radius — the only near-square control in the system.
