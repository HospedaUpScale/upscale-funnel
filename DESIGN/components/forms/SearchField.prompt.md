The top-bar search: pill, magnifier, and ⌘ F keycaps on the right.

<SearchField width={300} onChange={handleSearch} />

Copy is always "Search anything". Keycaps render in Space Grotesk on a grey chip. This is the only pill-shaped input in the system.
